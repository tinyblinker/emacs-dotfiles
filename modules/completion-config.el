;;; -*- lexical-binding: t -*-

;; Completion defaults
(setq tab-always-indent 'complete   ;; Tab: indent or complete
      completions-format 'vertical) ;; vertical list

(provide 'completion-config)
;;; completion-config.el ends here
