;;; -*- lexical-binding: t -*-

;; Global completion defaults (shared by vertico/corfu/orderless)
(setq tab-always-indent 'complete   ;; Tab = indent if at indentation, else complete
      completions-format 'vertical) ;; Display completions in a vertical list

(provide 'completion-config)
;;; completion-config.el ends here
