;;; -*- lexical-binding: t -*-

;; ox-gfm: GitHub Flavored Markdown export backend for org-mode
(use-package ox-gfm
  :ensure t
  :after org)  ;; load only once org is loaded (it's an org export backend)

(provide 'ox-gfm-config)
;;; ox-gfm-config.el ends here
