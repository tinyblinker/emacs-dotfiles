;;; -*- lexical-binding: t -*-

;; So-long: use fundamental-mode for files with very long lines
(use-package so-long
  :config (global-so-long-mode 1))

(provide 'so-long-config)
;;; so-long-config.el ends here
