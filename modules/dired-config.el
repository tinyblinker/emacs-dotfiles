;;; -*- lexical-binding: t -*-

;; Directory editor
(use-package dired
  :custom
  (dired-dwim-target t)           ;; dwim target
  (dired-auto-revert-buffer t))   ;; auto-revert

(provide 'dired-config)
;;; dired-config.el ends here
