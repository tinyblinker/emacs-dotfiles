;;; -*- lexical-binding: t -*-

;; Repeat: repeat commands by pressing only their last key (e.g. C-x o o o)
(use-package repeat
  :config (repeat-mode 1))

(provide 'repeat-config)
;;; repeat-config.el ends here
