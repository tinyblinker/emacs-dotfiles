;;; -*- lexical-binding: t -*-

;; Annotate candidates
(use-package marginalia
  :ensure t
  :defer 1
  :config
  (marginalia-mode 1))

(provide 'marginalia-config)
;;; marginalia-config.el ends here
